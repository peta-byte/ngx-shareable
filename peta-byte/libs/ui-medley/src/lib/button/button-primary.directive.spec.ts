import { ButtonPrimaryDirective } from './button-primary.directive';

describe('ButtonPrimaryDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonPrimaryDirective();
    expect(directive).toBeTruthy();
  });
});
